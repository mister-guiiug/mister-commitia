{
  "name": "mc-desktop-e2e-native",
  "private": true,
  "version": "0.1.0",
  "description": "E2E desktop natifs (tauri-driver / WebDriver) — paquet autonome, non installé par le front principal.",
  "scripts": {
    "test": "wdio run wdio.conf.cjs"
  },
  "devDependencies": {
    "@wdio/cli": "^9",
    "@wdio/local-runner": "^9",
    "@wdio/mocha-framework": "^9",
    "@wdio/spec-reporter": "^9"
  }
}
